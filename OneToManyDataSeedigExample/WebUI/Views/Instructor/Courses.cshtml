@model List<Course>

@{
    Instructor instructor = ViewBag.Instructor;
}

<h1>Instructor Information</h1>

@instructor.FirstName <br>
@instructor.LastName <br>
@instructor.Email <br>


<h1>Courses</h1>


@if(Model != null){

    if(Model.Count == 0){
        <p>Kayıt bulunamadi.</p>
    }

    @foreach (Course course in Model)
    {
        @course.Title <hr>
    }

}
